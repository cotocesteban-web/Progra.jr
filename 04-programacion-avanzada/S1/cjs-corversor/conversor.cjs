// Definimos las funciones
const metrosAKilometros = (m) => m / 1000;
const kilometrosAMetros = (km) => km * 1000;

// Exportamos usando el objeto module.exports
module.exports = {
    metrosAKilometros,
    kilometrosAMetros
};